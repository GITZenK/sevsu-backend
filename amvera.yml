meta:
  environment: python
  toolchain:
    name: pip
    version: 3.10

build:
  requirements: requirements.txt

run:
  script: uvicorn backend:app --host 0.0.0.0 --port 8000
  persistence: []
  container_port: 8000